# Builds the landing page (site/) and the MkDocs manual into one tree and
# publishes it to GitHub Pages.
#
# Layout: the manual builds into build/docs, then site/* is copied over the
# top, so the landing page serves at / and the manual at /docs/. Same shape as
# script.easytv and script.easymovie.
#
# No "Configure Pages" step, on purpose: actions/configure-pages calls GET
# /repos/{owner}/{repo}/pages, which throws on a repo with no Pages site
# enabled, such as Rouzax/Machinon, a force-synced mirror, and this job's
# read-only contents:read token would not have permission to call it anyway.
# None of its outputs (base_url, origin, host, base_path) are used below: no
# static_site_generator is set, and mkdocs.yml already hardcodes site_url.
#
# The deploy job runs on the upstream repo ONLY. Rouzax/Machinon is a
# force-synced mirror with no Pages site, so an unguarded deploy step would
# fail on every sync. The build job carries no such guard and runs on both
# repos, so a broken docs build is caught on the mirror too.
name: Deploy Docs

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install pinned dev tools
        run: pip install -r requirements-dev.txt

      - name: Build the manual and assemble the site
        run: |
          mkdocs build --strict -d build/docs
          cp -r site/* build/
          cp -r fonts build/fonts
          touch build/.nojekyll

      - name: Upload the Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: build/

  deploy:
    needs: build
    if: github.repository == 'domoticz/Machinon'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
